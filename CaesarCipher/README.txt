password: cnss{1s_7h9t_TF}

使用IDA反编译，查看加密逻辑，只对字母和数字做了处理。程序将输入字符串和flag逐字符比较，遂使用OD查看内存，可以看到被加密的密码。为分析加密逻辑，我使用16 * 'a' 和 16 * '5' 作为输入，对比IDA反编译和OD的汇编，容易看出两个全局变量的初始值分别为2和4，再查看测试字符串的密文，容易得出加密逻辑，于是对flag字符串的密文进行还原。将破译后的flag明文输入，提示错误，甚是不解，发现密码中有3个字母的破译出了偏差(+1s)，数字的破译完全正确，那么必定是加密者在字母加密上做了手脚。想到凯撒加密存在一个回绕操作，那么不难推测，回绕一定有问题。使用 16 * 't' 输入，果然发现，字母表的最后两个字母被无视了——字母移位和传统凯撒加密算法有些不同。再次查看IDA，原来模数是24（有26个字母，可是我记错了）